<%= form_for(state) do |f| %>
  <% if state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(state.errors.count, "error") %> prohibited this state from being saved:</h2>

      <ul>
      <% state.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :icao24 %>
    <%= f.text_field :icao24 %>
  </div>

  <div class="field">
    <%= f.label :callsign %>
    <%= f.text_field :callsign %>
  </div>

  <div class="field">
    <%= f.label :origin_country %>
    <%= f.text_field :origin_country %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
